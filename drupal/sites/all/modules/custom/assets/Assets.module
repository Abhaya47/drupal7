<?php


function assets_menu()
{
    $items = [];

    $items['assets'] = [
        'title' => 'Assets',
        'page callback' => 'assets_page',
        'access callback' => TRUE,
    ];

    return $items;
}
